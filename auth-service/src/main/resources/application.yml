server:
  port: 8381

jwt:
  # Keep this value aligned with gateway-service. Nacos wifi-jwt.yml can override it.
  secret: YourSecretKey-AtLeast256Bits-ChangeInProduction!
  expiration-millis: 86400000
