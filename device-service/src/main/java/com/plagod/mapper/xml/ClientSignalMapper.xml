<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.plagod.mapper.ClientSignalMapper">

    <select id="selectTrendBuckets" resultType="com.plagod.vo.device.SignalAnalyticsSourceVO$SignalTrendBucket">
        select
            from_unixtime(floor(unix_timestamp(report_time) / #{bucketSeconds}) * #{bucketSeconds}) as bucket_time,
            count(*) as sample_count,
            avg(rssi) as average_rssi,
            min(rssi) as min_rssi,
            max(rssi) as max_rssi
        from t_client_signal
        where node_id = #{nodeId}
          and mac = #{mac}
          and report_time &gt;= #{startTime}
          and report_time &lt;= #{endTime}
        group by bucket_time
        order by bucket_time
    </select>
</mapper>