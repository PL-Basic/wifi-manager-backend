<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.plagod.mapper.NetworkEntitlementMapper">

    <update id="deductRemainingSeconds">
        update t_network_entitlement
        set remaining_seconds = remaining_seconds - #{seconds}, version = version + 1, update_time = current_timestamp
        where entitlement_id = #{entitlementId} and status = 1 and #{seconds} > 0 and remaining_seconds >= #{seconds}
    </update>

</mapper>