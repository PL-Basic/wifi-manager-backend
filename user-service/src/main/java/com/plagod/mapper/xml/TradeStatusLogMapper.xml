<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.plagod.mapper.TradeStatusLogMapper">
    <insert id="insertIgnore">
        insert ignore into t_trade_status_log (
            business_type, business_no, event_key, from_status, to_status,
            operator_type, operator_id, remark, create_time
        )
        values (
            #{businessType}, #{businessNo}, #{eventKey}, #{fromStatus},
            #{toStatus}, #{operatorType}, #{operatorId}, #{remark},
            #{createTime}
         )
    </insert>
</mapper>
