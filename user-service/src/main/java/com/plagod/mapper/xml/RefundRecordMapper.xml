<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.plagod.mapper.RefundRecordMapper">

    <insert id="insertOrResolveExisting">
        insert into t_refund_record (
            refund_no, order_no, payment_no, purchase_id,
            user_id, request_id, channel, status, reason,
            requested_seconds, requested_amount_cents,
            refunded_seconds, refund_amount_cents,
            reviewer_id, reviewer_name, review_comment, review_time,
            channel_refund_no, channel_event_id, channel_payload_hash,
            failure_message, complete_time, version,
            create_time, update_time
        )
        values (
                   #{refundNo}, #{orderNo}, #{paymentNo}, #{purchaseId},
                   #{userId}, #{requestId}, #{channel}, #{status}, #{reason},
                   #{requestedSeconds}, #{requestedAmountCents},
                   #{refundedSeconds}, #{refundAmountCents},
                   #{reviewerId}, #{reviewerName}, #{reviewComment}, #{reviewTime},
                   #{channelRefundNo}, #{channelEventId}, #{channelPayloadHash},
                   #{failureMessage}, #{completeTime}, #{version},
                   #{createTime}, #{updateTime}
               )
        on duplicate key update refund_id = last_insert_id(refund_id)
    </insert>

</mapper>